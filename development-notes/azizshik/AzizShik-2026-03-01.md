# Дата: 2026-03-01

**Что было сделано:**
- Создал класс `Router` для управления хэш-навигацией в проекте
- Реализовал базовую поддержку следующих маршрутов: start, auth, dashboard, done, day/1–day/7
- Добавил методы `init()`, `navigate()` и обработку события `hashchange`
- Настроил парсинг пути из URL и генерацию хэша из объекта Route
- Подключил роутер к коллбэку `onRouteChange`, чтобы приложение могло реагировать на смену маршрута
- Закоммитил и запушил файл `src/router/router.ts`

**Решения (или попытки):**
- Выбрал хэш-роутинг (window.location.hash), т.к. проект пока без сервера и это самый простой способ
- Сделал fallback на главную страницу (`start`), если приходит неизвестный путь
- Проверял работу вручную: переходы по ссылкам, прямой ввод адреса в браузере, обновление страницы

**Мысли / Планы:**
- Теперь есть рабочий механизм навигации — можно начинать привязывать к нему экраны и компоненты
- В будущем можно будет доработать: добавить защиту роутов, параметры, 404-страницу


**Затраченное время:** ~2 часа