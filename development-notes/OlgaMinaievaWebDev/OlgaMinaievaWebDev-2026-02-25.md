# Дата: 2026-02-26

**Что было сделано:**
Сегодня полностью настроила автоматический деплой проекта на GitHub Pages через GitHub Actions. Добавила workflow `deploy.yml`, который собирает приложение и публикует его после мерджа в `main`.  
Создала `vite.config.ts` с корректным `base`, чтобы сборка правильно работала на GitHub Pages.  
Также пришлось немного поправить ESLint — добавила override для `vite.config.ts`, чтобы правило `import/no-extraneous-dependencies` не мешало работе.

После этого протестировала CI и убедилась, что деплой проходит успешно и сайт открывается по ссылке:
https://olgaminaievawebdev.github.io/rs-tandem-devquest/

**Проблемы:**
Во время настройки возникла ошибка ESLint, из-за которой pre-commit блокировал коммит. Пришлось разобраться, почему правило конфликтует с Vite-конфигом.

**Решения (или попытки):**
Добавила точечный override в `.eslintrc.cjs` только для файла конфигурации, чтобы сохранить строгие правила для основного кода. После этого проверила работу lint-staged и pre-commit — всё прошло успешно.

**Мысли / Планы:**
Теперь, когда инфраструктура настроена (CI + деплой), можно переходить к архитектуре проекта: реализовать Store и EventBus.  
Также нужно добавить ссылку на деплой в README и проверить, что все требования чекпоинта выполнены.

**Затраченное время:** около 4 часов
