# Дата: 2026-03-01

## Что сделано

Сегодня занималась ревью и install Sass.
- Проверила PR с Router локально (роуты, refresh, back/forward — всё работает).
- Разрешила конфликт в `main.ts` (объединили Router и SCSS).
- Настроила protection для `feat/phase-1-skeleton` (PR + approval + checks).
- Почистила репозиторий:
  - убрала случайный `node_modules` в root,
  - удалила дублирующийся `development-notes/app`.
- Синхронизировала ветки и проверила `typecheck`.

## С чем столкнулась

- Конфликт между ветками sass и router.
- Один раз установила зависимости не в ту директорию.

## Выводы

Стала лучше понимать, как работать с ветками, diff и конфликтами.  
Важно всегда следить, из какой директории запускаю команды.

## Время

~2–3 часа