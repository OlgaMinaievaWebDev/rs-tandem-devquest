**Дата:** 2026-03-26

## Что было сделано
- Добавил получение пользователя через `store.getState()` и отображение имени и аватара в сообщениях.
- Убрал лишние обращения к DOM для оптимизации производительности.
- Привёл все функции к единому стилю — заменил обычные функции на стрелочные.
- Создал `scrollUtils.ts` и улучшил поведение скролла при появлении новых сообщений, добавил плавную анимацию перехода.
- Исправил баг, когда первое сообщение скроллилось только до середины, внедрив паттерн двойного `requestAnimationFrame` для корректного reflow.
- Подключил подсветку кода через `highlight.js` для сообщений с кодом.

## Решения
- Использование `afterRender` с двойным `requestAnimationFrame` позволило корректно прокручивать чат до конца даже при первом сообщении.
- Вынес логику скролла в отдельный модуль `scrollUtils.ts` для переиспользуемости и поддержки плавной анимации.
- Применение стрелочных функций и оптимизация DOM повышает читаемость и стабильность кода.

## Планы
- Интегрировать реальные ответы от AI.
- Добавить `DOMPurify` для безопасного вставления HTML в чат, чтобы предотвратить XSS.
- Добавить индикатор «Boss is typing…» для улучшения UX.

**Время:** ~5 часов