# Дата: 2026-03-28

- **Что было сделано:** 1. Написала EventBus TypeScript (Issue #3). Использовала дженерики, чтобы была строгая типизация и TS сам подсказывал названия событий. 2. Внедрила EventBus в код: теперь клик по карточке игры не требует прокидывания функции через три файла. Компонент просто «кричит» о событии, а `main.ts` его слушает. 3. Начала делать Day System (Issue #7) — «мозг» игры, который следит за правилом «2 из 3 выполненных задач». 4. Реализовала блокировку уже пройденных задач, чтобы игрок не мог накликать одну и ту же игру дважды.

- **Проблемы:**

1. _Устала от Prop Drilling:_ передавать коллбэки глубоко вниз по дереву компонентов.
2. _Риск повторного прохождения:_ поняла, что игрок может случайно или специально кликнуть на уже решенную задачу.

- **Решения:**

1. _EventBus:_ полностью решил проблему пропсов. Теперь компоненты независимы и общаются через события.
2. _Умные и глупые компоненты:_ чтобы заблокировать пройденные задачи, я не стала пихать логику Стора внутрь отрисовки. Я прокинула массив `completedTasksToday` как пропс из `main.ts` в `dayMain.ts`. Теперь компонент отрисовки "глупый": он просто смотрит в массив, и если задача там есть — аппаратно блокирует кнопку (`disabled = true`) и добавляет визуальный стиль.

- **Мысли / Планы:** ⏱️
  - Implement Per-Task Timer #8
  - 🧠 Implement Quiz Widget #9

- **Затраченное время:** 5 часов
