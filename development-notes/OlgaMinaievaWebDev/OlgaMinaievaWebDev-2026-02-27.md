# Дата: 2026-02-27

**Что было сделано:**
- Сегодня добавила `core/state.ts` — это теперь наша база, наш «контракт» состояния.  
- Определили структуру `AppState`, `Route`, `User`, `GameState` и задали `initialState`.  
- Теперь у команды есть чёткая точка опоры, от которой можно строить Store, Router и UI.

**Митап с командой:**
Провели встречу и обсудили, как будем двигаться дальше.

Зафиксировали MVP:
Start → Day 1 → вопрос → ответ → Done → Restart.

Решили:
- Держать состояние централизованно (Single Source of Truth).
- Пока не добавлять EventBus — не усложнять раньше времени.
- Разделить работу по слоям и по файлам, чтобы не конфликтовать в PR.

Распределили задачи:
- Store — Ирина  
- Router — Азиз 
- UI экраны — разделили по файлам Ольга/Ирина

**Проблемы:**
- Нужно было уточнить структуру User и убрать лишние поля

**Решения:**
- Сделали name опциональным
- Не добавляли authForm в глобальное состояние (MVP решение)

**Мысли / Планы:**
- Следующий шаг — реализация Store
- Затем подключение Router

**Затраченное время:** ~ 3 часов