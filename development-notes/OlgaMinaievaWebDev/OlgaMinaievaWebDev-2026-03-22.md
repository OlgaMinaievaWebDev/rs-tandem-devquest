# Дата: 2026-03-22

## Что сделано

Сегодня работала над реализацией выхода пользователя (Sign out).

- Добавила кнопку **Sign out** в интерфейс (dashboard / header).
- Реализовала метод logout:
  - очистка текущей сессии пользователя
- Настроила редирект:
  - после выхода пользователь возвращается на стартовый экран (`start`)


## С чем столкнулась

- Важно было учесть, что пользователь может вручную изменить URL, поэтому проверка доступа должна работать через router.


## Выводы

Стало понятнее, как строить полный auth flow: вход, хранение состояния и выход.  
Также лучше поняла, как связаны router, store и UI при изменении состояния пользователя.

## Время

~1–2 часа